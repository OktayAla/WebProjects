// Bu JavaScript dosyası, web sitesinin ana işlevselliğini kontrol eden temel dosyadır.

document.addEventListener('DOMContentLoaded',function(){initSmoothScroll();initSkillAnimations()});function initSmoothScroll(){document.querySelectorAll('a[href^="#"]').forEach(anchor=>{anchor.addEventListener('click',function(e){e.preventDefault();const targetId=this.getAttribute('href');const targetElement=document.querySelector(targetId);if(targetElement){window.scrollTo({top:targetElement.offsetTop-50,behavior:'smooth'})}})})}
function initSkillAnimations(){const observer=new IntersectionObserver((entries)=>{entries.forEach(entry=>{if(entry.isIntersecting){const progressBars=entry.target.querySelectorAll('.progress-bar');progressBars.forEach(bar=>{const width=bar.style.width;bar.style.width='0%';setTimeout(()=>{bar.style.width=width},100)})}})},{threshold:0.2});const skillsSection=document.querySelector('.skills');if(skillsSection){observer.observe(skillsSection)}}
document.addEventListener('DOMContentLoaded',function(){document.querySelectorAll('.progress-bar').forEach(bar=>{const skillPercentage=bar.parentElement.previousElementSibling?.querySelector('.skill-percentage');if(skillPercentage){const percentage=skillPercentage.textContent;bar.style.width='0%';setTimeout(()=>{bar.style.width=percentage},500)}})});if(typeof AOS!=='undefined'){AOS.init({duration:800,easing:'ease-out',once:!1,mirror:!0})}